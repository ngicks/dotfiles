# Socket dir for the libvirt-sandbox user session daemon. Published outside
# /run/user/<uid> (which is 0700) so members of the libvirt-sandbox group --
# and devenv containers via --group-add keep-groups -- can reach the daemon.
d /run/libvirt-sandbox 2770 libvirt-sandbox libvirt-sandbox -
