{
  "tasks": {
    "install": "deno run -R -W -E=HOME,XDG_CONFIG_HOME,XDG_CACHE_HOME,USERPROFILE,AppData,LocalAppData ./src/install.ts",
    "update:daily": "deno run -R -W -E=HOME,XDG_CONFIG_HOME,XDG_CACHE_HOME,USERPROFILE,AppData,LocalAppData --allow-run=git,powershell.exe ./src/update_daily.ts",
    "devenv:build": "deno run -P=devenv:build src/devenv_build.ts",
    "devenv:build:bump": "deno task devenv:build --bump"
  },
  "imports": {
    "#/": "./src/"
  },
  "permissions": {
    "devenv:build": {
      "read": ["./devenv_ver"],
      "write": ["./devenv_ver"],
      "run": ["podman"],
      "env": [
        "HTTP_PROXY",
        "HTTPS_PROXY",
        "http_proxy",
        "https_proxy",
        "NO_PROXY",
        "no_proxy",
        "SSL_CERT_FILE"
      ]
    }
  }
}
